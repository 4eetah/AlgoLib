template <typename T>
class BST
{
    template <typename T>
    struct Node
    {
        T val;
        Node *left, *right;
        Node(T v) : val(v), left(nullptr), right(nullptr) {}
    };
public:
   

};
