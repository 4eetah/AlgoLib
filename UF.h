#pragma once

/* UnionFind data structure.
 * It supports the union and find operations, along with
 * a connected operation for determining whether two sites
 * are in the same component and a count operation that returns
 * the total number of components
 */
class UF
{
}
